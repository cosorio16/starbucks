---
const { titleProduct, imageProduct, nameProduct } = Astro.props;
import menu from "../data/menuData";
---

<div class="food_container p-5 lg:px-20">
  <h3 class="font-bold text-lg">¡Disfrútalos!</h3>
  <h1 class="font-extrabold text-2xl mb-6">
    Conoce nuestras bebidas y alimentos de temporada
  </h1>
  {
    menu.map((item) => (
      <>
        <div class="title">
          <p class="font-bold text-lg border-y border-zinc-400 py-2 tracking-wide">
            {item.title}
          </p>
        </div>
        <div class="products_container grid grid-cols-2 p-12 gap-16 lg:grid-cols-3">
          {item.products.map((product) => (
            <div class="container_produc basis-1">
              <div class="card_product flex flex-col gap-5 items-center">
                <div class="img_product">
                  <img src={product.img} alt="" width="130px" />
                </div>
                <p class="text-center font-bold text-lg tracking-wide">
                  {product.name}
                </p>
              </div>
            </div>
          ))}
        </div>
      </>
    ))
  }
</div>

<style>
  .img_product {
    border-radius: 50%;
    background-color: #1e3932;
    position: relative;
    width: 150px;
    height: 150px;
    cursor: pointer;
    outline: 1px solid rgba(0, 0, 0, 0);
    transition:
      transform 0.333s ease,
      outline 0.333s ease-in-out;
  }

  .img_product img {
    width: 130px;
    height: 130px;
    position: absolute;
    left: calc(50% - 65px);
    top: calc(50% - 70px);
    transition: transform 0.333s ease;
  }

  .img_product:hover {
    transform: scale(0.95);
    outline: 10px solid #2e564c5a;
  }

  .img_product:hover img {
    transform: scale(1.05);
  }
</style>

<script></script>
